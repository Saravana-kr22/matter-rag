# Matter RAG — System Prompt Custom Instructions
#
# This file is appended to the LLM system prompt during PR analysis (after the
# spec reference sections).  Add cluster-specific rules, house style notes, or
# any additional guidelines the model should follow.
#
# The entire file is sent verbatim — Markdown is fine.
# Leave this file with only comments to inject nothing extra.
#
# Examples:
#   - "Always include a PICS condition on every test step."
#   - "Prefer adoc 'NOTE:' blocks for informative observations."
#   - "Target Matter SDK CHIP-Test-Harness style step numbering."
